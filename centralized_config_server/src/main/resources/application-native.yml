server:
  port: 8888
spring:
  application:
    name: application-centralized-config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:///${user.home}/IdeaProjects/cycle_dev_project/config/cloud/{application}/{profile}
