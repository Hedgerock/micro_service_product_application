server:
  port: 8081
spring:
  application:
    name: application-catalogue-service
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://hedgerock:12345@localhost:8888